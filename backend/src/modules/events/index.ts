/**
 * Register cross-module event listeners.
 *
 * Subphase 1: empty registration so the lifecycle hook in app.ts is wired now.
 * Subphase 4+: handlers attach here.
 */
export function registerEventListeners(): void {
  // No-op for Subphase 1.
}
