import { PlaceholderPage } from '@/components/placeholder-page';

export default function HomePage(): JSX.Element {
  return (
    <PlaceholderPage
      name="Home"
      subphase={5}
      description="The public reader experience — hero, article grid, category rails, and ad slots — lands in Subphase 5 alongside the public reading APIs."
    />
  );
}
