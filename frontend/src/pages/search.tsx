import { PlaceholderPage } from '@/components/placeholder-page';

export default function SearchPage(): JSX.Element {
  return (
    <PlaceholderPage
      name="Search"
      subphase={5}
      description="Full-text search across articles, with filters and result ranking, ships in Subphase 5."
    />
  );
}
