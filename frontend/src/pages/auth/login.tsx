import { PlaceholderPage } from '@/components/placeholder-page';

export default function LoginPage(): JSX.Element {
  return (
    <PlaceholderPage
      name="Login"
      subphase={2}
      description="Email + password sign-in with social providers, the OTP flow, and Stay-logged-in arrive in Subphase 2 alongside the auth + users + organisations modules."
    />
  );
}
