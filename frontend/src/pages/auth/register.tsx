import { PlaceholderPage } from '@/components/placeholder-page';

export default function RegisterPage(): JSX.Element {
  return (
    <PlaceholderPage
      name="Create account"
      subphase={2}
      description="Sign-up with name + email + password + mobile, social provider continuation, and account verification land in Subphase 2."
    />
  );
}
