import { PlaceholderPage } from '@/components/placeholder-page';

export default function ForgotPasswordPage(): JSX.Element {
  return (
    <PlaceholderPage
      name="Forgot password"
      subphase={2}
      description="Password reset over email + OTP arrives in Subphase 2."
    />
  );
}
