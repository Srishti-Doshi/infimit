import { PlaceholderPage } from '@/components/placeholder-page';

export default function AdminDashboardPage(): JSX.Element {
  return (
    <PlaceholderPage
      name="Admin dashboard"
      subphase={4}
      description="System controls — create authority, manage editors, institutes requests, calendar, comment approval — land in Subphase 4."
    />
  );
}
