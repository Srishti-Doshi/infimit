import { PlaceholderPage } from '@/components/placeholder-page';

export default function ReaderDashboardPage(): JSX.Element {
  return (
    <PlaceholderPage
      name="Reader dashboard"
      subphase={4}
      description="Bookmarks, reading history, notification preferences, and saved searches arrive in Subphase 4."
    />
  );
}
